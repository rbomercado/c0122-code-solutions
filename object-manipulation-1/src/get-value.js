/* exported getValue */
/*
function that retrieves values at the key property
inputs will be an object and key
output will be a value
prodcue value at the key property
*/
function getValue(object, key) {
  return object[key];
}
